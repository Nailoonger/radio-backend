# =================================================================
# 根目录 .dockerignore —— 给后端镜像用
# 关键：必须排除 data/mysql/（含 .sock）
# =================================================================
data
uploads
*.sock
*.pid
*.log
deploy/certs
admin-web
miniprogram
tests
docs
.git
.gitignore
.dockerignore
.env
.env.example
README.md
*.md
*.yml
*.yaml

# 依赖改为容器内 npm ci 安装，主机的 node_modules 不进构建上下文
node_modules
