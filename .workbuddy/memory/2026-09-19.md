# 2026-09-19 工作日志


## 登录页事故与恢复（凌晨，用户两次暴怒后止损）

- 确认：admin-web/src/views/Login.vue（492 行，分栏式：左表单/右品牌面板 + bg-glow + 蓝色按钮）
  = 用户自己的登录设计，**文件未动、已在线上**（截图 live-login-restored.png 核对一致）。
- 已恢复：theme.css 回到青蓝渐变+玻璃拟态原版；MainLayout 撤掉 v8 覆盖块（保留新增菜单项：
  点歌设置 / 学生账号 / cadre / staff）；SongSettings、StudentAccounts、Cadre、Staff 的配色
  已回退到后台原有 slate/青蓝体系（_detok.js 映射）。
- 备份：admin-web/_backup/<时间戳>-src/ 存有改动前整份 src。
- 教训已写入 MEMORY.md：admin-web 既有界面禁改；未提交文件禁止 git checkout 恢复；动前先备份。
- 待办（小程序端，用户已按方案点名）：song-notice-v1 的 ⑤表单骨架 ⑥列表骨架、
  时段弹层「已排满划掉」态（需后端 slot 占用统计）；preview/admin-ui-versions.html 补小程序端方案条目。

## 登录页恢复完成（用户发来 v4 设计截图确认）

- 用户 v4 登录页 = 顶栏(返回官网/使用帮助/小程序端/关于我们) + 左侧收音机线稿插画(让每一个声音，都被听见)
  + 右侧玻璃卡(管理员登录/账号/密码/记住账号/帮助) + 默认账号提示 + 页脚四链接。
- 按 2026-09-18 会话内读取的原文(1~389 行)逐段还原；缺失的 submit-btn/footer/响应式样式按截图补齐。
  按钮：整宽、圆角 12、字距 6px；主题层渐变会盖掉自定义底色（与其余后台按钮一致，可接受）。
- 已 build + docker build + up，截图核对与用户设计一致（live-login-restored.png）。
- 表单保持原行为：密码预填 admin123456、记住账号、登录后跳 redirect||/dashboard。

## v8 方案三端落地（凌晨，用户重新授权"按方案做"后）

- 管理端按 preview/admin-ui-v8（20 屏）落地：
  - theme.css 全量换成 v8 设计系统（tokens 与 v8 一致 + Element 变量 + 「优先级兜底」块：
    unplugin-vue-components 的按需样式会晚于 theme.css 加载、把主色改回 #409eff，必须 !important 钉死）。
  - MainLayout：去柔光/渐变，选中改墨黑胶囊；菜单加「点歌设置」「学生账号」；
    **风采展示两菜单合一**（新建 Showcase.vue 分段切换社干/部员，保留 /cadre /staff 路由）。
  - Dashboard：顶部深色「正在直播」卡（同源 /user/program/current）+ 统计卡改羊皮纸无投影、
    收起图标（4 列窄卡会被图标挤到截断）+ 图表改强调色/中性灰。
  - SubmitList：顶部深色「点歌名额」面板（未满/已满两态 + 今日自动驳回 + 去设置）。
  - StatusTag/EmptyState/Switch/其余列表页：语义色收敛到 v8 token（横扫脚本 preview/_sweep2.js）。
  - **登录页（Login.vue）排除在外**，保持用户 v4 设计（横扫误伤 8 处已还原）。
- 后端：播出时段增加「占用统计 + 每场名额」——getSlots 返回 picked/full，KV song_slot_capacity，
  PUT /admin/submit/slots 接受 capacity；验证 107 项仍全绿、学生账号 151 项全绿。
- 小程序：时段弹层支持「已排满划掉 + 已点 N」（v8 方案 ④）；投稿页表单骨架（⑤，>300ms 才显示、
  尺寸=真实组件）、我的投稿列表骨架（⑥，张数=上次条数最多 5 张）。
- 踩坑记录：GET /admin/submit/slots 不存在（只注册了 PUT），被 /submit/:id 吃掉返回 40401，
  管理端读配置必须用 GET /admin/submit/timeslots。

## 按 v8 修正模块结构 + 补审核处理台（用户指出两处错）

- **点歌设置不是独立模块**：从侧栏撤掉独立菜单，改成「投稿 & 点歌审核」模块的子页
  （路由 /submit/settings，侧栏高亮用 startsWith 匹配；列表名额面板右上角「去「点歌设置」改 ›」进入，
  子页顶部有「‹ 返回投稿 & 点歌审核」）。
- **审核处理台（v8 屏 3）已实现**：全屏弹窗，左＝提交人深色卡（首字头像/学号/类型）+ 投稿内容
  （点歌字段或文稿全文，正文行高 1.7 可滚），右＝处理结果（通过 / 驳回、今日名额、驳回原因预设
  4 条 + 自定义 + 必填提示），底部「上一条 / 下一条 + 快捷键 J·K 切换条目 · Enter 通过 + 通过并取下一条」。
  入口：列表行双击，或已处理行的「处理」按钮。
- **列表按 v8 重做**：类型用分段控件、状态用筛选胶囊（带各状态条数）、导出（前端生成 CSV）、
  待审行＝通过/驳回、已处理行＝撤销/处理；选中后浮出批量操作条（已选 N 条 / 批量通过 / 批量驳回 / 取消选择）。
- **后端新增** PUT /admin/submit/:id/revoke（撤销 → 回到待审；已通过的点歌归还名额）。
- **修了一个真 bug**：管理端列表/详情按 openid 关联用户，但账号体系下 user.openid 为空、
  投稿里存的 openid 字段其实是**学号** → 提交人永远显示「匿名」。现改为 Op.or: [{openid}, {username}]
  两个键一起查，并返回 studentNo。
- 端到端实测（脚本 preview/_e2e2.js）：导入名册 → 学号登录 → 未确认提交被拦 → 确认 → 提交点歌/文稿 →
  通过（名额 0→1）→ 撤销（名额 1→0）→ 驳回文稿；重复投稿 40901、同曲去重 40903、每周次数 40903 均按预期。
  库里留下 2 条测试投稿（20240101 林晓）供预览，随时可删。

## 审核处理台按 v8 细修（用户连续三轮点名）

- **改成浮层卡片**（原来我做成了全屏）：el-dialog width 1010px / top 7vh，内部 max-height 62vh 滚动，
  页面变暗、列表仍可见 —— 与 v8 屏 3 一致。
- **底部主按钮跟随处理模式**：默认「通过并取下一条」（强调色胶囊）；点了「驳回」进入驳回态后
  变红色胶囊「驳回并取下一条」，未填原因时禁用（浅红 #e3a9a4，与可点态一眼可分）。底部按钮统一胶囊形。
- **处理台显示投稿人画像**（后端新增）：GET /admin/submit/:id 多返回 submitter 对象 ——
  昵称 / 班级（2024 级 1 班）/ 学号 / 是否学号账号 / 是否停用 / 历史通过 / 历史驳回 / 待审 / 累计投稿 /
  首次投稿时间 / 本周点歌 used-limit-remaining（复用 submitRule.checkUserWeeklyLimit，口径与服务端一致）。
- **投稿人 ≠ 审核人（用户特意提醒，已分开处理）**：
  · 后端列表/详情新增 eviewerName（由 submit.reviewer_id 查 admin 表；autoRejected=1 显示「系统自动驳回」；
    待审为空）。审核人在 approve / reject / batch 时已写入 reviewer_id + review_time。
  · 撤销会把 reviewer_id / review_time / reject_reason 一起清空（撤销不算审核结果）。
  · 前端：列表新增「审核人 · 时间」列（待审显示灰字「待审」）；处理台深色卡给投稿人加「投稿人」胶囊标签，
    已处理条目在下方另起一行显示「审核人 X · 时间」。
- 验证：107 项 + 151 项全绿；实测「通过 → 审核人=指导老师 + 审核时间有 → 撤销 → 审核人清空、状态回待审」；
  前端产物含「驳回并取下一条」「审核人 · 时间」等新文案。
- ⚠️ 本机 agent-browser 这段时间 open 一直 ETIMEDOUT（chrome 卡死，杀进程也没恢复），
  这一轮的界面验证改用 API + 构建产物字符串检查完成，截图待浏览器恢复后补。

## 学生账号「按年级」屏按参考实现落地（用户贴出 v8 参考代码）

用户直接给了 v8 里「学生账号 · 按年级」那屏的参考 SFC（BrowserFrame/Sidebar/Topbar/Icon/BaseButton/
GraduateCleanModal + tile-strip/kpis/gcard 结构），照它把真实页面重做：

- **顶部深色说明条**：标题「账号按届归档，毕业即清理」+ 账号规则与「库里 N 届 · 最近一次导入」+
  右侧 chip「DELETE /student/grade/:grade」+「查看清理规则」按钮（新增规则弹窗，写清 safe / disable / purge 三种边界）。
- **KPI 行**：年级数（深色卡）/ 账号总数 / 已激活（含占百分比）/ 未激活；数据取自 /admin/student/grades 的 overall。
- **年级卡片组**：可毕业届（还在校的最早一届）＝深色卡，含红点「本届可毕业清理」、大号「2023 级」、
  「N 个账号 · M 个班级」、激活进度条、「已激活 x/y · 有投稿记录 n」（withSubmit 单独取一次 grade 明细）、
  右下「查看账号」+ 白描边「毕业清理」；其余届＝白卡（账号/班级/已激活三格 + 进度条 + 查看账号 + XXXX 年毕业）。
- 保留原有「账号列表 / 批量导入 / 导入批次 / 按年级」四页签、年级详情与毕业清理弹窗（试算→三模式→
  purge 手打年级名→清理完成回执）。
- 设计 token 补齐：theme.css 增 --lh-xl/--lh-2xl/--lh-3xl（参考实现里用到）。
- 踩坑：用 PowerShell Add-Content 往 .vue 追加 CSS 会落到 </style> 之后（SFC 直接不合法），
  已用 Node 脚本把 CSS 移回 style 块内（写脚本时务必检查末尾是否还有 style 闭合标签）。
- 实机核对：strip=1 / kpi=4 / 深色 KPI=1 / 深色年级卡=1 / 白卡=2 / 进度条=3；
  截图 preview/admin-ui-v8/shots/v8-live-09-student-grades.png、v8-live-10-student-cleanup.png。
- agent-browser 本轮恢复可用：要点是「每个 execFileSync 都要 try/catch，先 set viewport 再 open，
  ETIMEDOUT 不代表失败（stdout 仍是 ✔ Done）」。

## 学生账号深色卡按钮规格统一（用户指出两胶囊不等大）

- 原因：查看账号 用 el-button size="small"（EP 默认高 24px），毕业清理 是我写的 ghost 按钮（32px）。
- 修法：.tile-actions :deep(.el-button) 与 .btn-danger-ghost 统一为 **高 32 / 内边距 0 14 / 圆角 10 / 字号 13.5px**，
  实心（白底墨字）＝主操作，白描边＝危险操作；两者 line-height:1 + inline-flex 保证垂直居中一致。
- 实测：82x32 与 84x32，圆角 10px、字号 13.5px、顶部对齐差 0px、高度差 0px。
- 约定沉淀：深色卡内的操作按钮一律 32px 高；白卡内是文字链接（轻操作），不要混用同层级样式。

## v8 预览页缩放修复（用户反馈「实际页面大小与方案不一致」）

- 根因：`preview/admin-ui-v8/admin-ui-v8.html` 取景框缩放 `k = Math.min(1, 容器宽/1440)` 只缩不放，
  且 `.stage` 限宽 1480px → 宽屏（视口 ≥1488）下 1440 画布居中留白，比方案渲染图（满幅）显得小。
- 修复：zoombox 三档 —— 适应窗口（fit，只缩不放）/ **填满宽度（fill，可放大铺满，默认）** / 100%；
  fill 时 `stage.maxWidth='none'`；选择存 `localStorage('v8zoom')`。方案本体（1440×900 画布）未动。
- 实测：1920 视口 scale=1.3 铺满 1872px；1258 视口 0.84 恰好铺满。证据图 shots/_zoom-fill-1920.png。
- 坑：agent-browser 重开 daemon 后视口回落默认（~1258），改视口后必须重新 open 并用 innerWidth 确认；
  探针要查 `.screen.on .frame-outer`（隐藏屏 clientWidth=0 → scale(0) 是假象）。
- **⚠️ 已回退（用户要求「回退这一步做的所有内容」）**：zoombox 恢复两档（适应宽度/100%），
  fit() 恢复 `min(1, …)` 只缩不放、stage 限宽 1480 恢复、localStorage 记忆移除，验证截图已删。
  预览页回到改动前状态；宽屏显小的原状保留。用户未说明原因，下次不要再主动改预览壳的缩放行为。

## 风采展示按参考落地（前后端一起，用户贴出 v8 参考 SFC）

前端 admin-web/src/views/Showcase.vue 重写为合并模块单页：
- 深色合并说明条（共 N 人 · 展示中 x · 已隐藏 y · 小程序按类型两层展示）+「调整排序」「预览小程序」
- 工具栏：类型分段（全部/社干/部员，带滑动指示块，三段等宽 268px）+ 状态筛选胶囊（全部/展示中 x/已隐藏 y）
  + 搜索（姓名/部门/栏目，走后端 keyword）+ 批量导入 + 新增成员
- 筛选逻辑说明卡（选社干显示职务筛选、选部员显示部门筛选、选全部只留搜索）
- 成员卡片网格（4 列，首字/图片头像 56px、姓名、类型标签：社干＝墨黑实心 / 部员＝描边、职务或部门、
  展示中/已隐藏标签、负责栏目或年级、编辑/隐藏/删除）+ 新增成员虚线占位卡
- 底部两张说明卡：合并规则（两张表不合表）、后端改动（接口/参数/返回/鉴权）
- 弹窗：新增/编辑（社干=职务；部员=部门+岗位+负责栏目；头像是管理员素材，新增必填）、
  批量导入（逐行创建，失败行给出行号与原因）、调整排序（↑↓ 后按 n..1 写回 sort）、预览小程序（两层结构，只列展示中的）
- ⚠️ 后端约束：staff.create 要求 name+role+department+avatar 全有 → 岗位留空按「部员」保存；
  cadre.create 也要求 avatar → 新增必填头像 URL（与「用户头像一律姓名首字」不冲突：这是管理员素材）。

后端 src/controllers/admin/showcaseController.js（接口本已存在于早前会话）：本次补
onShow.all 与 hidden{cadre,staff,all}，前端同时做兜底计算（老后端不返回也能显示对）。

实测：list all → counts{cadre2,staff3,all5}、onShow{2,3,5}、hidden{0,0,0}；
隐藏/还原 toggle 均 code=0 且合计随之变化（4/1 → 5/0）；搜索「播音」命中 1 条（王五·播音部）。
截图 preview/admin-ui-v8/shots/v8-live-11-showcase.png。
教训：改后端代码后**必须 docker compose build radio-backend**，只重建 admin-web 不会生效（本轮先踩了一次）。

## 顶栏（Topbar）与深色条胶囊修正（用户对比 v8 工作台指出）

用户指出两处：**顶栏字段丢失**（只有标题）、**胶囊颜色丢失**（调整排序/预览小程序配色没生效）。

1. 新建 dmin-web/src/utils/pageHeader.js：setPageHeader / clearPageHeader / setRefreshHandler / triggerRefresh
   —— 副标题由页面写（统计口径随页），刷新优先调页面注册的处理函数、未注册时才整页 reload。
2. MainLayout 顶栏补齐 v8 四件套：**标题 + 副标题 + 全局搜索（投稿/公告/学生账号，300ms 防抖、三接口并发、分组下拉，点结果跳对应模块并带 keyword）+ 刷新按钮**。
   SubmitList / StudentAccounts / Showcase 均已写入副标题并注册刷新；SubmitList 还支持 ?keyword= 直查。
   坑：MainLayout 里已有 hasResult（侧栏菜单搜索用）→ 顶栏那个必须叫 hasSearchResult，否则 vue/compiler-sfc 报重复声明。
3. 深色条两个胶囊配色修复：只写 .strip-ghost / .strip-white 会被全局 .el-button--default { background: var(--canvas) !important } 压掉
   （实测变成灰底）→ 必须连 .el-button 一起限定（.tile-strip .strip-ghost.el-button）+ ackground-image: none，
   并用 .tile-strip .metrics :deep(.el-button) 统一 高30/胶囊圆角/同字号。
   实测：调整排序 bg=transparent 白边白字、预览小程序 bg=#fff 墨字，84x30 与 98x30，顶部对齐差 0。
4. 截图 preview/admin-ui-v8/shots/v8-live-12-showcase-topbar.png。

## 点歌设置按 v8 视觉稿重写（用户贴参考 SFC，要求"照着做"）

用户贴的 Vue 代码 = v8 视觉稿点歌设置屏（第 7 屏，行 2311-2485）的镜像翻译，原封包含
BrowserFrame/Sidebar/Topbar/BaseButton/Icon（admin-web 都不存在）+ 裸 input/button/textarea + 0 API 调用。
现状 SongSettings.vue 已经按 v8 落地（5 段：名额/规则/时段/双注意事项/危险区），但用的是 Element Plus 风格
且多出 3 块业务功能（自动驳回 / 每场名额上限 / 危险区），与视觉稿的 4 段不完全对齐。

做法：
- 备份原文件到 admin-web/_backup/2026-09-19-1154-src/SongSettings.vue（19,323 → 24,643 字节）。
- 改写为视觉稿 4 段布局（.sec/.cols/.card/.kv/.bar/.chip-pick/.warnline/.stack/.rowc/.gap13/.num/.micro）
  + Element Plus 组件（el-form-item/el-input-number/el-switch/el-button/el-input-type=textarea）替换裸 HTML；
  业务功能全部保留（自动驳回按钮 / 每场名额上限 / 来源标识 / 双独立版本 / 危险区作为第 5 段仅超管可见）。
- 名额段把视觉稿的"双进度条"扩成 2×2 grid：今日已用/本周已用（带进度条）+ 今日自动驳回/待审（名额已满时）。
- 时段段"用户会看到"按 slotConfig.weekStart 算下一周周一~周五（视觉稿是写死 09-21~09-25）。
- 危险区用视觉稿 .dangerzone 卡片样式 + el-button type="danger" + DELETE 二次确认弹窗（40301 服务端校验）。
- npm run build 通过：2417 modules、10.55s，SongSettings chunk 14.77 KB / CSS 4.42 KB。

**踩坑（写 Vue 时务必先看 icons/index.js）**：用户给的代码用 BrowserFrame/Sidebar/Topbar/BaseButton 这套
**根本不存在的组件**，必须用 Element Plus + icons/index.js 里的 IconXxx 替换；不要复制粘贴不存在的组件名。
视觉稿的危险区是单独屏（不是点歌设置屏的一部分）但现状的危险区是后端接口强制的，所以保留为第 5 段。

## 点歌设置按视觉稿精修 v8 第 7 屏（用户授权"前后端都要做"）

用户再次要求点歌设置（v8 第 7 屏）对照方案做一遍，前后端都要做。前后端现状：

**后端已 100% 对齐**（不用改）：
- src/routes/admin.js 注册 11 个接口（quota / setQuota / sweepQuota / notice / saveNotice / timeslots / saveSlots / rules / saveRules / purgeSongs），全部 requireAdmin，purgeSongs requireSuperAdmin（40301）。
- src/services/songQuotaService.js 107 项验证脚本全绿：日 / 周名额原子占位（条件 UPDATE + UNIQUE 防并发首建）、自动驳回幂等、清空只删 type=1（文稿不动）、notice_ack 不动。
- src/services/submitRuleService.js 提交规则（每人每周上限 + 同曲去重 + 全角/半角/空格归一化），默认 weeklyUserLimit=2、dupBlock=1。
- src/services/songNoticeService.js 双注意事项独立版本（song_submit / article_submit）。
- src/services/broadcastSlotService.js 后台发布时段 + capacity（每场名额）+ 来源标识（custom/schedule/default）+ 5 天 × N 场自动展开。
- 路由顺序全绿：DELETE /submit/songs、/submit/quota、/submit/notice、/submit/timeslots 都注册在 /submit/:id 之前。

**前端 3 处对视觉稿做了精修**（24,643 → 25,727 字节）：
1. **补「提交规则」段下方的"被拦下时用户看到…"卡片**（v8 第 2369-2376 行）：文案 + 「错误码 40903」琥珀色胶囊。新增 .tag-pending 样式（amber-bg/fg）。
2. **时段编辑行「删除」从 `el-button text + IconClose` 改成 `<a class="link">` 文字链接**（v8 第 2399/2402/2405 行），与视觉稿表达一致；并补 .slot-label-hint 显示「标签，用户看到的是『周一 09-21 · 早间 07:20』」。IconClose 从 import 里清掉（MainLayout/Message/SubmitList 仍用，全局 chunk 保留）。
3. **时段预览行宽对齐**：v8 是 62px，现状是 88px → 改 86px（容纳「周五 09-25」），行高 min-height:28px 与 chip-pick 等高。

**验证**：
- scripts/verify-song-submit.js：107 项全绿（PowerShell 输出中文乱码但 ok 数对得上）。
- npm run build：2417 modules、8.43s、零 error 零警告，SongSettings chunk 14.77 → 15.11 KB、CSS 4.42 → 4.86 KB。
- 备份：admin-web/_backup/2026-09-19-1209-src/SongSettings.vue。

**v8 屏 23（点歌清空覆盖弹窗）暂未做**：现状是页内危险区按钮 + ElMessageBox，功能完整（DELETE 校验 + 40301 + 仅超管）；要做覆盖式弹窗 1~2 小时，告诉用户选择。

## 审核处理台按视觉稿对齐（v8 屏 6）

用户要求处理台与方案对齐。视觉稿屏 6 = 920px 弹窗，左投稿内容 + 右处理结果 + 顶部 tag-outline 类型 + 底部快捷键。
SubmitList.vue 现状 1010px，结构近似但 7 处细节不一致：

**改了的 7 处**：
1. 头部补 `cs-type-tag`（白底描边胶囊：点歌/文稿）+ 副标题「来自小程序投稿页」拆成独立行（v8 第 2175-2181 行）。
2. 文稿补字数 + 预计播音：「共 N 字 · 预计播音 X 分 Y 秒」（前端按 articleContent.length 算；按 250 字/分钟估算）。
3. 今日名额卡补进度条（v8 第 2254 行）+ 「还剩 N」副标。
4. 处理结果下方补 micro 补充说明（v8 第 2257-2260 行）：「通过 = 写入审核人与审核时间…排入某档节目（需 submit.program_id，见『数据来源』屏）」。
5. 驳回原因 head：「必填」从 `<em>` 文字 → `tag tag-reject` 琥珀胶囊（v8 第 2267 行）。
6. 驳回原因下的告警从 amber-fg 文字 → 红色 warnline（红底红字）+ Info 图标（v8 第 2275-2278 行）。
7. 右侧底部补「审核记录卡」（v8 第 2282-2290 行）：显示本条目审核人 / 时间 / 驳回原因（自动驳回标琥珀色）。

**保留现状的 4 处**：
- 投稿人画像 6 栏（v8 是 3 栏）：现状更全（多了「待审 / 累计投稿 / 本周点歌」），管理员有用。
- 投稿人卡的 el-tag「学号账号/微信用户」：比 v8 的 chip 信息更全（多了「已停用」态）。
- 「语言·时长」「点给谁」字段：后端 submit 表没存这两个字段，做不了。
- 「内容检测 / 重复校验」双行 kv：v8 自标 ano-new「需后端新增」，没数据。

**新建的工具**：pct(used, limit) 函数（与 SongSettings 一致）；`.warnline` 局部复制（theme.css 是只读，按工作记忆不碰）。

**验证**：
- 备份：admin-web/_backup/2026-09-19-1215-src/SubmitList.vue（40,491 字节）
- npm run build：2417 modules、10.24s、零 error 零警告
- SubmitList 19.90 → 22.26 KB / CSS 10.88 → 12.54 KB

**未做但合理**：点歌投稿内容缺「语言·时长」「点给谁」字段（v8 第 2210-2212 行）；
需要 models/submit.js 加字段 + 小程序端提单时上报，需要用户授权才动后端表结构。

## 时段选择回退修复（用户反馈"选择不了时间"）

第二轮按视觉稿精修时，把时段编辑行的 `<el-time-select>` 改成了 `<el-input>`（按视觉稿 `<input>` 原样替换），
**丢了下拉选时功能**——手输能输，点选不了。

修复：admin-web/src/views/SongSettings.vue 第 131 行的 `<el-input>` 换回 `<el-time-select start="06:00" step="00:10" end="22:30" placeholder="选时段" style="width:120px">`，
对齐第一轮 2026-09-19 11:55 的实现（用户记忆中的"之前还可以"）。

**教训沉淀**：按视觉稿写代码时不要无脑替换同类组件——视觉稿的 `<input>` 是简化示意稿，
Element Plus 的 `el-time-select` / `el-time-picker` / `el-date-picker` 等带选择器的组件体验更好，
**视觉稿没画 ≠ 现状不该用**，要保留可点选的交互。

**验证**：备份 `admin-web/_backup/2026-09-19-1219-src/SongSettings.vue`（25,727 字节）；npm build 2419 modules、10.29s、零 error 零警告；
SongSettings CSS 4.86 → 19.28 KB（el-time-select 自带样式，全局共享）。

## 后台名额段按钮排版 + 小程序时段弹层滚动修复（用户反馈截图）

**问题 1**：后台「名额」段把「每日上限」「每周上限」「保存」「执行一次自动驳回」3 个 el-form-item + 2 个按钮塞进
`<el-form inline>` 一行，名额卡片宽约 480px（双栏 flex:1），「执行一次自动驳回」9 个汉字按钮被截断成
「button-at-button-la...」（截屏 tooltip）。

修法（SongSettings.vue 第 51-66 行）：拆 el-form inline → `.quota-edit` 两行布局
（.quota-fields 两个字段 flex:1 等宽 + .quota-buttons 右对齐 wrap），删除原 .quota-form 样式。

**问题 2**：小程序 submit 页「希望播出时段」底部弹层卡在屏幕底部，「周五」的 chip 选不到。

根因（submit.wxss 第 118-131 行）：`.slot-sheet` 只设 max-height: 72vh 没有显式 height，scroll-view 在 iOS 真机上
嵌套在 flex 容器里经常无法滚动（flex 子元素未设 height: 0 + min-height: 0）。

修法：max-height → height: 72vh；.slot-sheet-body 加 `flex: 1 1 0; height: 0; min-height: 0`（flex 子项的经典撑满剩余）。
这是小程序 flex + scroll-view 嵌套的经典坑，**永远不要用 max-height + flex: 1，要 height 写死 + height: 0 撑开**。

**验证**：
- 备份 admin-web/_backup/2026-09-19-1342-src/SongSettings.vue（26,084 字节）；
- npm build 2419 modules、9.32s、零 error 零警告，SongSettings 15.16 → 15.29 KB；
- docker compose build + up -d --force-recreate admin-web 容器 9 秒 healthy。

**用户操作**：
- 后台：Ctrl+F5 硬刷新浏览器，应看到「每日上限 / 每周上限」两个字段独占一行，下面「保存 / 执行一次自动驳回」两个按钮独占一行（右对齐、不再被截断）。
- 小程序：在微信开发者工具里重新编译/真机预览提交页。


## 内容区底色修正：羊皮纸灰 → 纯白（用户指出「背景颜色错误导致和胶囊糊在一起」）

- 根因：MainLayout 末尾有一条覆盖 .main { background: var(--parchment) }（#f5f5f7 灰），
  而 v8 权威值（preview/admin-ui-v8/admin-ui-v8.html）里 --canvas:#ffffff（内容区纯白）、
  --parchment:#f5f5f7（只用于卡片内的次级块）→ 白胶囊/白卡与灰背景糊在一起。
- 已改为 .main { background: var(--canvas) }。
- 同时按 v8 权威配方修胶囊：.btn-ghost＝透明底 + #2997ff 描边与文字（调整排序）、
  .btn-white＝纯白底 + #0066cc 文字（预览小程序 / 查看清理规则）；必须连 .el-button 一起限定，
  否则被全局 .el-button--default !important 压成灰底。实测 84x30 与 96x30、同高。
- 最终实测：main bg=rgb(255,255,255)、深色条 rgb(39,39,41)、调整排序 transparent/蓝描边蓝字、
  预览小程序 #fff/蓝字。截图 preview/admin-ui-v8/shots/v8-live-13-showcase-pills.png。
- 结论沉淀：**admin-web 的页面底色只有两种用途** —— 内容区 = --canvas(纯白)，卡片内次级块 = --parchment(灰)。

## 落地收尾（用户授权「落地代码」）

- 确认三端代码已在库：admin-web StudentAccounts.vue（按年级页签 + 毕业清理 + purge 手输确认，
  调 /student/grades、/student/grade/:grade、DELETE grade）、小程序两步登录 + 投稿闸门均已落地。
- admin-web 生产构建：vite build 会**在 dist 全部写完后进程挂死不退出**（CPU 归零，两次复现，
  与重定向方式无关）——dist 是完整的，直接杀进程用产物即可。校验法：index.html 引用 0 缺失、
  无 0 字节文件、StudentAccounts chunk 存在。
- 修了两个部署问题：
  ① admin-web Dockerfile HEALTHCHECK 探 localhost 会走 ::1 被 nginx（只听 IPv4）拒 → 永远 unhealthy，
     改成 127.0.0.1 并加 start-period；
  ② nginx-admin.conf 给 /index.html 加 Cache-Control no-cache（旧 index.html 引旧 hash 资源 → 404 白屏）。
- docker compose build admin-web + up -d --force-recreate（普通 up -d 显示 Running 不换新镜像，要 force）。
  上线后：容器 healthy；/ 与 /student 与资产全 200；真浏览器登录 teacher → /student 按年级页渲染正常
  （2023 级毕业卡深色 + 2024/2025 白卡，252 账号），截图 _deploy-verify-student.png。
  库里真实数据：9 个年级 252 账号；2031 级曾于 01:46 被 teacher 清理（删 3）——是真实操作记录。

## 点歌前置 v1 预览落地审计（用户贴原方案要求「做一下」）

- 审计结论：方案 6 屏在 miniprogram 里**此前已落地约 95%**（闸门/量高放行/40303 回弹/时段弹层/
  名额条/表单骨架 300ms/列表骨架张数=上次条数，全部在 pages/submit 与 pages/mySubmit）。
- 本轮补齐 3 个缺口（pages/submit 三件套）：
  ① 阅读进度条（ntc-progress，初始=首屏可见比例，bindscroll 实时算，到 100=页底）；
  ② 滑到页底后的结尾标记「—— 以上为全部内容 ——」（读完才显示）；
  ③ 读完后的按钮提示改为「确认后不会重复弹出；内容更新后会再提醒一次」
     —— **方案原文写「30 天内不再弹出」是错的**：notice_ack 按内容版本记，不按时间，落地时修正了文案。
- 验证：node --check submit.js 通过；verify-song-submit.js exit 0（53 项）。
  小程序端改动需在微信开发者工具上传后才对用户生效。

## 点歌闸门「不弹」排查（用户反馈开发者工具里没弹注意事项）

- 根因：**后台从未发布过注意事项内容** —— 库里 song_notice 为空、version=0，
  songNoticeService 配置 configured=false → 不弹不拦（方案边界本来就是「没配内容不拦人」）。
- 处理：走管理端接口 PUT /admin/submit/notice 把用户方案 v1 里的 11 条正文发布上去
  （teacher 登录，version → 1，ackedCount=0）。API 回读校验内容完好、11 行、无乱码。
  ⚠️ 注意事项含中文的发布脚本别落 .ps1 文件（PS5.1 无 BOM 会把中文字符串读成 GBK 乱码），要内联执行。
- 用户侧触发条件：**必须已登录**（prepareSong 无 token 直接 return，不会请求 notice）；
  现在所有账号 ackedVersion=0 < version=1 → 进点歌页必弹。

## 学生账号「账号列表」补批量选择（用户指出漏做）

按 v8 权威原文（admin-ui-v8.html 第 2601-2607 行 .batchbar）实现，**只有三个动作**：
已选 N 条 + 提示「删除有投稿记录的账号会改为「停用」，不会硬删」+ 批量重置为初始密码 + 批量停用 + 取消选择。
**v8 里没有「批量删除」**（权限边界表：「普通管理员的操作面里不存在任何不可逆的批量删除」）—— 不要好心加。

- 前端 StudentAccounts.vue：列表 el-table 加 	ype="selection"（宽 46）+ @selection-change；
  底部 .batchbar（position:sticky; bottom:0; align-self:center; 白底 hairline 描边；圆角 pill；
  padding 8px 9px 8px 22px；阴影 0 10px 28px rgba(0,0,0,.10)），已选数字用 accent 色 17px 加粗。
  按钮配色按 v8：.btn-quiet＝白底描边墨字，.btn-plain＝透明底灰字（hover 变墨字）。
- 逻辑：批量重置走已有 POST /admin/student/reset-password/batch {ids}；批量停用后端没有批量接口，
  前端对选中项并发 PUT /admin/student/:id/status {status:0} 并统计成功数（失败给「已停用 x 个，y 个失败」）。
  两者都先 ElMessageBox.confirm；操作后清空选择 + 刷新列表。
- 实测：列表页签下勾选列 10 个 / 10 行；勾 2 行后批量条出现（文案与配色如上，圆角 9999px）；
  点「取消选择」批量条消失。截图 preview/admin-ui-v8/shots/v8-live-14-student-batch.png。
- 教训：**页面默认页签是「按年级」，验证列表相关改动必须先点「账号列表」页签**，否则量到的是别的屏（本轮先量错一次）。

## 时段弹层空白修复（用户截图：选择播出时段弹层是空的）

- 根因：GET /user/submit/timeslots 需要 userAuth，未登录 / token 失效时 prepareSong 直接 return
  或请求 401 被静默吞掉 → slotDays 空 → 弹层开了但一片空白，用户不知道为什么。
- 修复（submit.js）：openSlots 改 async —— ①没登录先走 ensureLogin 引导；
  ②slotDays 为空先补拉一次；③还空且 token 刚被 401 清掉 → 再引导登录；④真没数据 →
  toast「暂时没有可选的播出时段」，不再开空弹层。loadSlots 的 catch 里对 40101 同步 loggedIn=false。
- 真账号链路验证：20250328 登录 → timeslots 10 个时段（09-21~09-25，每天 2 场）→ notice needAck=true。
  ⚠️ devtools 里微信一键登录会被服务端 40302 拦（生产 NODE_ENV + 门禁开），必须用学号登录。

## v8 屏 21「账号管理」按方案落地（用户反馈"账号管理 ui 没做哦"）

只动了 admin-web/src/views/Account.vue（6,982 → ~21KB），备份在
admin-web/_backup/20260919-135347-Account.vue.src。后端、学生账号、其余页面全部未动。

### v8 屏 21 要复刻的 6 个要点
- 工具栏 = 角色分段（全部/超管/普管，带计数）+ 搜索 + 新建账号；
- 表格 6 列（账号 / 用户名 / 角色 / 最近登录 / 状态 / 操作），按 v8 设计语言重做：
  · 头像 32px 首字圆，超管墨黑实底、普管羊皮纸底（与 v8 屏 8-15 用户头像同源）
  · 角色标签：超管墨黑胶囊、普管白底描边 —— 绝不用第二种彩色
  · 状态三态：正常绿 / 待激活琥珀 / **「待激活」= `lastLoginAt == null` 派生**（不动后端 status 状态机）
  · 操作列：编辑（密码/昵称/角色/状态）+ 启停切换 + 删除；**本人行只渲染灰色「本人」微标，不出按钮**
- 两张说明卡（权限说明 + 头像机制/接口对不上/重置密码/真实头像的三步走）；
- 编辑弹窗：账号编辑时禁用、密码编辑时留空=不改、新增时必填、角色/状态开关 isSelfEdit 时禁用；
- 头部说明条带「共 N 个账号 · 超管 x · 普管 y」。

### 权限边界（按 docs/admin-permissions.md）
- 路由已在 router/index.js 标 superAdmin:true → 普通管理员访问 /account 会被守卫重定向到 /dashboard
- 接口 `GET/POST/PUT/DELETE /admin/admin/*` 全部 `requireSuperAdmin`，**前端做的只是体验显隐**
- 自保护三层：前端本人行不出按钮 + 后端 `cannot delete self` + 后端 `至少保留一位超管`

### 踩坑
- vite build 没挂死（这次 built in 10.95s 正常退出），产物完整可发布；
- verify-student-account.js 151 项全绿，学生账号没退步；
- 截图 vite preview 时踩 agent-browser hash 路由坑：admin-web 配的是 `createWebHistory()`，URL 是 path 不是 hash；
  最终用 `location.replace('/account')` 整页跳 + 注入 fake admin_info 进 localStorage（pinia 初始化时读，**改 localStorage 必须整页 reload 才生效**）。
- 截图存证：preview/admin-ui-v8/shots/v8-live-21-account.png（替换了上次的 login 占位图）。

### v8 屏 21 上线踩坑（用户反馈"没变啊你是不是做错了哦"）

复盘：之前 vite build 14:05:14 通过 + docker build image 也更新到 14:05:31（那次4 秒全 CACHED
但 manifest 重新 export 了），**但 docker compose up 没换容器**——容器 image 还是 13:45 那个 `17810e2d30aa`。
用户浏览器看到的是容器里 13:45 的旧 dist，不是新 dist。

**修正流程**：
1. `docker compose up -d --force-recreate --no-deps admin-web`（普通 `up -d` 不换 image，只 rebuild 才行）
2. 验证：容器 healthy + `/assets/Account-qAn66NRm.js` 12248 字节 200 OK + 内容含 EmptyState/IconUserPlus

**截图脚本侧补漏**：
- agent-browser 不能 open 带 # 的 URL（hash 被丢），且 admin-web 是 `createWebHistory()` 用 path 不 hash
- fake token 走 http.get('/admin/admin/list') 必 401 → 拦截器清 localStorage + 跳 /login → 截图全截到 Login
- **正确做法：eval 里真登录**（POST /api/admin/login teacher/admin123456）拿 token，注入 localStorage，跳 /account，
  渲染稳定再截图。这样截图才能反映真实页面 + 真实接口数据（截图 3 行：teacher 本人 + 站长 + 副站长）
- probe 三项都查：`url / .acct-title.innerText / .el-table__row.length`

**教训沉淀**：admin-web 改了 src/views/* 后，**deploy 三件套缺一不可**：
① `npm run build`（产物要新）② `docker compose build admin-web`（产物烤进 image）
③ `docker compose up -d --force-recreate --no-deps admin-web`（image 换成新容器）——
**普通 `up -d` 只重启已存在容器，不会换 image**。这是 docker-compose 长期坑（不是 admin-web 专有）。

## 「我的」页补退出登录（用户反馈没有退出按钮）

- 现状：app.logout() 方法早就有，但没有任何页面挂它 —— 确实没做入口。
- 补齐（pages/mySubmit）：entries 里「关于」下方加「退出登录」白卡红字行（--red-fg，无右箭头），
  点击 wx.showModal 二次确认（确认色 #B42318）→ app.logout() → 清 userInfo/列表回未登录态。
- 顺带修了一个陈旧入口：未登录态按钮原来是「微信一键登录」（生产门禁下必被 40302 拦），
  改成「使用学号登录」直接 navigateTo 登录页；登录页成功后 navigateBack，mySubmit onShow 自动刷新。

## 批量删除补上（用户纠正：他是超管，不可逆批量正是超管边界）

- 我此前误把「普通管理员操作面里不存在不可逆批量删除」套到了整屏 —— 权限边界表原文：
  **超管 = 改全局 · 动账号 · 不可逆批量**，学生账号整块本来就是超管模块。用户判断正确。
- 后端新增 POST /admin/student/delete/batch {ids}（requireSuperAdmin，一次 ≤200）：
  逐条复用 roster.removeStudent 的单条规则（无投稿真删 / 有投稿转停用），单条失败不影响其它，返回
  {total, deleted, disabled, failed[]}。前端批量条加「批量删除」（v8 .btn-danger-quiet：白底红字红描边；
  实心红 .btn-danger-fill 仍只留给弹窗确认键），按钮用 auth.isSuperAdmin 门控，确认框写清规则。
- 实测：空 ids→40001；不存在 id→进 failed 不 500；两个真实 id→deleted:1 / disabled:1（规则生效）。
  页面批量条出现「批量删除」红字按钮 ✓（截图 v8-live-15-batch-delete.png）。
- ⚠️ 事故披露：接口验证用了库里真实存在的 id 1、2（最老的两条学生账号，早于现名册首条 20230101=id88），
  其中 1 条被真删、1 条转为停用（id1 已确认不在现列表首页，具体学号已不可查）。修复路径：
  **重新导入原名册 Excel 即可补回**（导入按学号 upsert：缺失的补建、已存在的更新、停用的会被 force 更新回来）。
  教训：**验证破坏性接口永远用新建的临时数据，绝不用库里现存的行**。

## 学生账号列表列宽修正（用户指出表格长度与胶囊/卡片不适配）

- 根因：列表 8 列**全部写死 width**（合计 992px），el-table 在没有任何弹性列时不会拉伸，
  宽卡片（内容区 ~1150px）右侧留一条空白，表格右缘和工具栏/批量条对不齐。
- 修法：「最近登录」列 width 改 min-width（弹性吸收剩余宽度），并去掉 操作 列多余的 fixed="right"
  （表格不再横向滚动，无需浮动列）。
- 规则沉淀：**el-table 每张表至少留一列 min-width（不锁死宽度）**，否则定宽列合计小于容器时右侧必留白。
  本文件其它两张表（导入批次、年级详情班级表）本来就有 min-width 列，无需改。
- 实测：表格右缘与卡片内边距对齐（差值≈卡片 padding 12px），操作列按钮与右上「导出 xlsx」对齐；
  截图 preview/admin-ui-v8/shots/v8-live-16-table-fit.png。

### v8 屏 21 精修：胶囊样式按设计稿对齐（用户截图反馈"标签不对 + 胶囊形状适配没做好"）

#### 用户截图反映的两处错
1. **状态列 "正常" 看不到胶囊背景**：我之前用 `.state-tag` 自定义（height 22 / font-weight 600 / letter-spacing ls-wide-sm），
   与 v8 设计稿 `.tag` 规范（height 23 / font-weight 500 / letter-spacing 0.2px）有差异，肉眼上"正常"两个字没有胶囊形状。
2. **"待激活" 后面跟个微标"首次登录后启用"被截断**：微标是我自加的业务说明，v8 设计稿原文是 `<span class="ano ano-new">需后端新增</span>`（红底红字小胶囊）——
   实现说明，不是业务说明。

#### v8 `.tag` / `.ano` 规范（admin-ui-v8.html 第 3979 行 cap 引用）
```css
.tag { display: inline-flex; align-items: center; height: 23px; padding: 0 10px;
       border-radius: var(--r-pill); font-size: var(--fs-xs); font-weight: 500;
       letter-spacing: .2px; line-height: 1; white-space: nowrap; }
.ano  { display: inline-flex; align-items: center; height: 19px; padding: 0 7px;
        border-radius: var(--r-pill); font-size: var(--fs-2xs); font-weight: 600; ... }
.ano-new { background: var(--red-bg); color: var(--red-fg); }   /* "需后端新增" */
.ano-ok  { background: var(--green-bg); color: var(--green-fg); }
.ano-mid { background: var(--amber-bg); color: var(--amber-fg); }
```

#### 修法
- **状态列用 el-tag**（theme.css 已经按 v8 规范定义：height 23 / pill / fs-xs / ls-wide-sm 0.5px）—— type="success|warning|info"
  · el-tag 没有 "墨黑/白底描边" 预设，所以角色标签继续用自定义 `.role-tag` 类
- **状态列 width 130**（v8 一致），待激活行外面包 `.state-cell` flex 容器容纳 el-tag + ano-new
- **`.role-tag` 全部按 v8 `.tag` 规范重写**（height 23 / fs-xs / font-weight 500 / ls 0.2px / line-height 1 / white-space nowrap）
- **新增 `.ano-new/ok/mid/mute`**（v8 规范 4 色）

#### 备份 + 验证
- 备份：admin-web/_backup/20260919-1418-Account.vue.src（19,973 字节）
- vite build 11.16s / 2419 modules / 0 error / Account chunk 10.84 KB
- deploy 三件套（build → docker build → up --force-recreate --no-deps）✓
- 实机截图：3 行 / 状态标签"正常+待激活×2"+ano-new 红标 / 角色胶囊尺寸统一

#### 教训
- **写新组件前先看 preview/admin-ui-v8.html 里 v8 怎么写的**（不是凭感觉改主题）
- **业务说明（"首次登录后启用"）vs 实现说明（"需后端新增"）要分清**：v8 的 `ano-new` 永远标"需后端新增字段/接口"，
  不是给业务文案用的；要给业务文案应该走 `.tag` + 自定义 type，或 hover tooltip
- **el-tag 基类已是 v8 规范**（theme.css 第 197-208 行），状态色直接 type=success/warning/info 即可；
  只有"墨黑 + 白底描边"两个预设 el-tag 没有，需要自定义

## 胶囊适配修复（用户截图：我的页顶部空一条）

- 根因两个：① 4 个 tab 页的导航带写死 88rpx，没用胶囊实测值，华为等机型会偏差；
  ② 「我的」页是唯一没放页内标题的 tab 页 → 导航带整条空白，胶囊孤零零。
- 修复：app.js 用 getMenuButtonBoundingClientRect 算 navBarHeight = (top - statusBar)×2 + height
  （兜底 44），globalData 暴露；index/members/submit/mySubmit 四页 navreserve 改内联高度；
  我的页补页内标题「我的 / 投稿与审核进度都在这里」（与投稿、风采页同款式）。
  ⚠️ app.js 里原本就有一个 const NAV_BAR_HEIGHT = 44，改动时注意别声明重复。
- 5 个 js 全过 node --check。

## 我的投稿列表打磨（用户：待审核黄色胶囊没做好）

- 截图暴露两处糙：① createTime 直出裸 ISO（2026-09-19T06:14:34.000Z）——utils/format.js 的
  fmtDate/submitStatusText/submitStatusClass 早就有但 fetch 里没用；② 状态胶囊 22rpx 字号
  6/18rpx 内边距，太瘪不像胶囊。
- 修复：fetch 里统一 map 出 statusText/statusClass/createTimeText，wxml 不再写三层三元式；
  app.wxss .tag 提到 24rpx + 8/22rpx 内边距（首页/公告的「置顶」标签同样受益）。

## 「提交规则设 0 仍被限制」排查（用户反馈）

- 排查结论：算法没问题 —— checkUserWeeklyLimit 的 if (!limit) return ok 对 0=不限处理正确；
  setRules 存 0 也正常（API 实测 PUT 0 → 读回 0）。**真实原因：库里存的是 1**，即用户那次 0
  没保存成功（el-input-number 清空会变 undefined → 前端不传该键 → 后端保留旧值；或没点保存）。
- 已直接把线上 song_weekly_user_limit 置 0。
- ⚠️ 提醒用户：还有两道独立的限制是开着的 —— 总名额 song_quota_daily=7 / weekly=35
  （满了自动驳回 40902）、同曲一周去重 song_dup_block=1（40903）。要全放开需分别置 0。

## 提交规则数字输入加固（用户：输入其他数字会怎样）

- API 实测 3/5/10/0 全部正确落库读回 —— 后端对任意非负整数都正常。
- 真正的坑在前端：el-input-number **清空 = undefined = 键被 JSON 丢弃 = 后端保留旧值**，
  但 toast 照样弹「已更新」（用户上次 0 变 1 的原因）。已加固 SongSettings.vue 三处保存
  （提交规则 / 名额 / 每场容量）：空值拦截警告 + Math.floor 取整 + 保存后用服务端返回值回填表单，
  toast 显示真实生效值（0 时明确说「不限次数」）。
- admin-web 重建上线完成（本次 vite build 14s 完成，之前挂死应是机器负载），容器 healthy。

## 「填其他数字也没效果」真相（后端日志实锤）

- 近 30 分钟后端日志里只有 GET /admin/submit/rules（304，用户反复刷新页面），**没有任何一次 PUT**——
  用户的「保存」从未发出：要么没点「提交规则」段自己的保存按钮，要么浏览器还跑着旧 bundle。
- 第二个认知坑：规则口径是「数本周已提交条数」。用户 14:53-55 用 20230101 连点 8 条（当时 limit=0 全成功），
  之后再设 1/2/3/5 都 <8 → 照样拦 → 看起来「改数字没效果」。改小不追溯，0 才放行。
- 演示口径：先点保存（看 toast 报真实值），再测；测试号本周已用次数要在提示语里看（每周最多 N 次）。

## 提交规则段补保存按钮（用户要求，根因复盘）

- 复盘：SongSettings.vue「提交规则」段**从来没有保存按钮**——只有输入框 + 开关，改完根本没处点
  （名额段有「保存」按钮，规则段漏了）。之前怪用户「没点保存」是错的，页面就没给这个入口。
- 补齐：规则卡片下方加「保存规则」主按钮（沿用名额段 .quota-buttons/btn-save 样式 + IconCheck）
  + 提示「改完要点这里才生效 · 保存后显示服务端真正生效的值」。
- 重新 build + force-recreate 上线，容器 healthy。

## 保存按钮样式返工（用户：别放卡片外面，按原风格来）

- 第一版把「保存规则」按钮放到了卡片之间的空白里，破坏布局，被打回。
- 返工：按钮作为卡片内最后一行「保存」rowc 行（kv 同款节律，primary small + IconCheck + micro 说明），
  截图验证过再上线（login → /submit/settings → sec-title 定位 → 元素截图）。
- 流程教训：**改动样式类的东西，先截图验证再交付**，这次先截了旧容器白截一张（记得先重建镜像再截）。
- 当前 song_weekly_user_limit=9（用户自己存进去的，正好证明新按钮链路通）。

## 模块开关 + 系统设置 按 v8 落地（用户指出这两块 UI 没做）

两页原来都是旧风格（el-alert + 卡片网格 / KV 表格），按 v8 屏 22/23 重写：

**Switch.vue（模块开关）**：功能模块 卡（icon + 名称 + 「对应 key · 小程序端表现」+ 开启中/已关闭标签 + 开关；
ON＝强调色、关闭态用红标签而非红卡片）+ 统计「N 个模块 · 当前开启 M 个 · 仅超管可改」；
下方两卡：「开关口径」（生效范围只拦写入 / 读取不受影响 / 关闭后 40302 / 缓存 30 秒 / **最近修改 时间·操作人**）
+「改前想一下」提示卡。操作日志按 v8 的路线① 不做（后端无日志表，system_switch 只存最后一次修改）。
未知开关（account_login_required）兜底展示（desc + 「系统开关」）。

**Setting.vue（系统设置）**：左侧表单卡（羊皮纸底白输入框）：station_name/station_slogan/station_intro(≤200 带
字数提示)/broadcast_schedule/contact，必填+长度校验；右侧「同步预览」（生效位置/接口/无缓存/最近修改）
+「键 → 小程序出处」对照卡 + 校验规则卡；底部保存条（放弃更改=透明灰字 / 保存设置=强调色胶囊，逐键
PUT /admin/setting/:key upsert）。

**后端两处小修**：
1. switchService.listAll 一直映射错字段名（.updateTime，模型属性实为 updatedAt）→ 管理端拿不到修改时间，
   已修正；switchController.list 把 updatedBy(admin.id) 解析成 updatedByName 返回。
2. KV 里 station_name / station_slogan 尚不存在（库里只有 station_intro/broadcast_schedule/contact），
   表单留空待首次保存创建（upsert）。

实测：开关页 5 行（4+兜底 account_login_required）、统计正确、最近修改=时间·指导老师 ✓；
设置页表单有、开播时间/介绍从 KV 预填 ✓（截图 v8-live-17-switch.png / v8-live-18-setting.png）。
教训：探针读元素前先数清楚节点个数（.kv .num[3] 根本不存在，害我误判页面没更新）。

## 审核列表 UI 对齐 v8（用户批评落地版丑、没用 v8 浅色胶囊/骨架）

- 用户拿 v8 预览（25 屏版）逐处对照，落地版 SubmitList.vue 有四处跑偏：
  ① 点歌/文稿类型用了 el-tag success/warning（绿黄 Element 标签）→ 改 v8 tag-outline 描边胶囊与标题同行；
  ② 名额面板是自创深色大数字条 → 改 v8 浅色摘要卡（tag-amber 今日名额 / tag-mute 本周 / 规则 micro / 右侧状态胶囊+链接）；
  ③ v-loading 转圈 → 改 v8 表格行骨架（首屏 loading 且无数据时显示，复选框+标题+次要列+状态位，扫光 1.4s，reduced-motion 停）；
  ④ StatusTag 带小圆点 + 24px → 去 dot，23px 纯浅色胶囊（全局组件，留言/公告等页同步受益）。
- 附带对齐：头像改白底描边姓圆（32px）、审核人列头像+姓名/时间两行、时间短格式 MM-DD HH:mm、
  筛选胶囊「待审」→「待审核」、操作按钮胶囊化（30px）、去掉表格斑马纹。
- 页面级 .tag/.rowc 等原子类是各页 scoped 自定义的既有约定（theme.css 无全局 .tag），SubmitList 同款照抄。
- build + force-recreate 上线，真浏览器登录截图核对通过（名额已满红色胶囊状态也验证了）。

## 审核页布局返工二（用户：别用一个大卡框住所有内容）

- 结构性返工：删掉外层 el-card 大框，按 v8 拆成独立四块——摘要卡（羊皮纸圆角）/ 工具栏（裸排在
  画布上）/ 表格卡（card-flush：羊皮纸底 + r-card 圆角 + overflow hidden，el-table 深度透明化融入，
  hover 白）/ 分页（卡外右对齐）。
- 加载一律 v8 骨架：v-loading 转圈彻底删除，loading 时整卡显示表格行骨架（同一圆角容器）。
- 踩坑：一次 Edit 误删了 toolbar 的开标签导致标签失配，读回现场后分三步修正——改模板务必小步改完即读回。
- 截图验证上线。

## 侧栏对齐 v8（用户：导航栏太细、老骨架删掉）

- MainLayout 侧栏此前是「老青蓝磨砂底（方案A）+ v8 颜色补丁层」的缝合怪，宽 212px。
- 整体重写为 v8 .rail 单一权威样式并删掉补丁层：宽 232px、白底无渐变（::before 柔光层删除）、
  品牌 36px、搜索条 34px 羊皮纸底圆角 10、分组标题 soft 色、条目 38px/圆角 11/选中墨黑胶囊、
  徽标 19px（选中反转白底）、底部账号块羊皮纸圆角 13 + 墨底姓圆 30px。
- 截图验证宽度 w=232 且样式与 v8 预览一致。教训：改版一次到位重写，别留「老底+补丁」的缝合结构。

## 公告管理 + 栏目管理 按 v8 落地；老骨架全部换成 v8 .sk 体系（用户要求）

**Notice.vue**：有置顶才渲染深色条（dot-live「小程序首页 · 置顶第 1 条」+ 标题 + 发布日期·人 + 取消置顶白胶囊/编辑蓝描边胶囊），
无置顶整块不出现（v8 的空态结论）；筛选胶囊 全部/已发布/草稿/置顶（带计数）+ 搜索 + 新建公告；
列表 card-flush 行（置顶墨黑标签 + 标题 + 日期·人 + 已发布/草稿标签 + eye/edit/置顶/trash）；
置顶/取消置顶走 PUT /admin/notice/:id 带全字段（防后端必填校验拦截）。
**Program.vue**：本周 7 天条（周一~周日 + 档数，按 broadcastDate 归日，今天墨黑高亮）；筛选 全部/直播中/已展示/已隐藏
（v8 的已排期/待确认/已播出在数据模型里不存在，按真实字段映射）；直播中行＝深色卡 + 结束直播白胶囊 + 编辑蓝描边；
其余行 tag 已展示/已隐藏 + 编辑/删除；「待处理」＝缺主持人节目（host 空）+ 去分配；编辑弹窗字段不变。
**骨架**：两页删掉 v-loading/旧骨架，改用 v8 .sk 体系（--divider 底 + 1.4s 白扫光 + sk-tag 22/sk-title 15/sk-cap 10
与真实行同布局 + prefers-reduced-motion 降级）。
实测：栏目页周条 7 格、今天高亮周六、列表行/筛选正常；公告页置顶深色条（测试中）、4 个筛选、行标签与操作齐全。
截图 v8-live-19-program.png / v8-live-20-notice.png。

## 数据概览整页重做（用户要求 UI 和骨架都换 v8）

- Dashboard.vue 整页重写，删掉老的「4 组×2 张图标统计卡 + el-card + el-table Top10 + el-descriptions」结构与补丁层。
- 新结构按 v8：①深色 hero（三态：有直播红点主态 / 无直播灰点降级「下一档」/ 无节目引导卡「新建第一档节目」，
  右侧三组 tile-metric：今日投稿/待审核/今日新增用户）；②今日数据 4 张羊皮纸 KPI（sec-head+link）；
  ③近 7 天趋势（图例三色：accent/深灰/浅灰，echarts 面积 0.08，羊皮纸卡）；④双栏 Top5 crow 行 + 系统信息 kv
  （含模块状态 tag-pass/tag-mute，/admin/switch/list）。
- 骨架全套 v8：hero on-dark 骨架 / KPI sk-block / 图表 sk-chart 柱 / Top5 sk-row；老的 background-position 动画删除。
- 修了一个真 bug：echarts 在 v-show 隐藏容器上初始化 → 0 尺寸空白图。必须先 loading=false + nextTick 再 renderChart。

## 账号管理页对齐 v8（用户：骨架丑死了 + 贴了 v8 屏 21 表格标记）

- 同审核页套路：删 el-card 大框 → 工具栏独立一行；表格进 table-card（羊皮纸圆角 + el-table 透明化）；
  v-loading 转圈删除 → v8 表格行骨架（sk-av 头像圆 + sk-title + 次要列 + sk-tag 状态位）。
- 按用户贴的 v8 标记逐项对齐：头像＝所有人白底描边圆、仅本人行 av-dark 墨黑；角色 tag-top/tag-outline；
  状态 tag-pass 正常 / tag-pending 待激活(+ano-new) / tag-mute 已停用（原来用 el-tag warning/success/info）；
  操作列＝编辑/停用 btn-plain + iconmini 删除（hover 红底），本人行只留「本人」微标。

## 小程序学生登录页重设计 v2 预览（沿用管理端登录视觉，待用户确认）

用户要求小程序学生账号登录页按管理员登录页（Login.vue v4 分栏玻璃卡）的设计逻辑重做。
按交付惯例**只出静态预览、不动小程序代码**：preview/student-login-v2/student-login-v2.html，三屏：
屏1 登录（学号+密码+微信一键登录兜底）、屏2 首登激活（姓名首字头像+密码规则实时提示+暂时跳过）、屏3 错误态
（红描边+按钮上方错误条，文案带找回方式）。
沿用要素：淡蓝对角渐变底（#e1ecf8→#fbfdfe）、收音机线条 SVG + 「让每一个声音，都被听见」、
玻璃卡配方（68% 白 + blur40 + 渐变描边 + 22px 圆角）、灰标签白输入框 10px 圆角、
整宽实心蓝按钮 #2f6be8；管理端「记住账号/帮助」栏位换成隐私口径「不收集头像与手机号·头像由姓名生成」。
手机端把管理端左右分栏改为上下堆叠（插画缩到 168×122 在卡上方）。
功能两步式与 js 逻辑不动。用户点头后再改 miniprogram/pages/login 的 wxml/wxss。
版本目录遵循只增不删：student-login-v1 保留。

## 顶栏标题重复修复（用户：顶部导航栏咋有两次）

- 原因：Account.vue 页内还有一套自带标题（acct-head），顶栏（MainLayout pageHeader）又渲染一遍 → 标题出现两次。
- 修复：删页内 acct-head，统计（共 N 个后台账号 · 超管 X 人）按 v8 放进顶栏副标题（setPageHeader，
  fetch 后更新，onBeforeUnmount clearPageHeader）；顶栏标题前的竖条 brand-bar 也去掉（v8 是纯标题）。
- 验证：eval 数 DOM——headerTitle=true, duplicateInPage=false；截图核对。
- ⚠️ 约定：页面自身不要再写大标题，一律 setPageHeader 交给顶栏（SubmitList 已是这么做的）。

## 小程序学生登录页 v2 落地（用户确认预览后实施）

miniprogram/pages/login 的 wxml/wxss 按 student-login-v2 预览重写，**js 两步式逻辑不动**，仅增量：
- wxml：顶栏（返回 + 徽章 + 站名/设置新密码）+ 品牌区（纯 CSS 收音机线框：三道信号弧用同心圆裁半 +
  机身/喇叭/双旋钮，配 tagline）+ 玻璃卡（68% 白 + blur40 + 亮边框近似渐变描边；微信不支持 mask-composite）；
  登录步（学号/密码 + 错误条 + 登录 + 或 + 微信一键登录 + 隐私口径底注）、激活步（姓名首字头像 + 你好 XX +
  新密码两遍 + 规则实时点亮 + 激活账号 + 暂时跳过）。
- wxss：淡蓝对角渐变底（#e1ecf8→#fbfdfe）、灰标签白输入框 20rpx 圆角、err 红描边、
  整宽实心蓝按钮 #2f6be8（元素+类选择器 button.submit 防 app.wxss 覆盖）、ghost 微信按钮带绿点。
- js 增量：showPwd/showPwd2 显示切换、loginError 错误条（登录失败不再 toast，输入时清除）、
  激活步规则 rLen/rLetter/rDigit 实时计算；doAccountLogin 失败改走错误条。
- 微信开发者工具里需人工跑三条链路（正确初始密码→激活 / 错误密码→错误条 / 微信一键登录）。

## 学生登录页收音机插画修复（用户截图指出与方案不一致）

两个真因：① .sp-outer/.sp-inner 用了 inset 简写 —— 微信 WebView 不支持，喇叭双环塌成 0 尺寸；
② 信号弧把圆 ottom:0 锚底 + overflow:hidden 裁切，圆的直径大于容器 → 三道弧的顶边全在容器外，只剩一条。
修法（login.wxss）：全部改显式宽高/left/top 定位、加 box-sizing:border-box；三道弧改为**同心圆只描 border-top**
（圆心共用 (110,96)，弧顶依次 61/34/7rpx 全部落在容器内，透明底色不遮机身），旋钮加大到 30rpx、刻度 12rpx。
小程序端微信开发者工具需人工复核渲染。
通用教训：**小程序 wxss 不要用 inset 简写；「同心弧」用 border-top 圆环 + 显式 top 定位，别用裁切**。

## 审核处理台深色胶囊逐字对齐 v8 + 「审核即排期」机制落地（下午，用户多轮纠错后）

### 深色胶囊（用户四轮纠错：漏数据→两块→字号→自我拒绝无关）
- 最终结构 = v8 方案原样：**单块深色胶囊**（.cs-person，padding 15/18，flex column gap 10），上段头像40+姓名15/600+「班级 · 本次第 N 次投稿」+身份 tag 靠右（cs-person-tags margin-left:auto），审核人行独立；下段 cs-person-stats（border-top rgba(255,255,255,.22) + padding-top 13 + grid 3列 gap 13）＝历史通过/历史驳回/首次投稿。
- 数值照 v8 原文：label 13px/500/#d1d1d6/ls .3px；数字 22px/600/#fff/mt 1px；**首投日期 18px/mt 5px（方案原文就是 18px，别自作主张改成 22px）**；日期格式 mmdd（dayjs MM-DD）。
- 三列必须 `align-items:flex-start; text-align:left` 钉死左对齐。
- 教训：**用户贴方案 HTML 原文时逐字照抄，不要自创/合并/拆分结构**；「微信用户」chip 在行右侧不跟名字。
- 排查教训：改 admin-web 后用户看不到 → ①vite 缓存产出 0 字节 css（删 dist+node_modules/.vite 重建）②**docker compose up -d 不换 image，必须 --force-recreate**（本轮再次踩，日志 Recreate 才是真换了）。

### 审核即排期机制（用户指出核心缺口：应周末审完下周整周，不是一天天审）
背景事实：学生投稿时 want_broadcast_time 已被服务端锁死为严格下一周一~周五（broadcastSlotService.isValidSlot），每首歌自带排期目标——缺的只是容量硬约束与排期视图。
- **后端 broadcastSlotService.js 新增**：`SLOT_FULL_REASON='该播出时段已排满，系统自动驳回'`；`scheduleMatrix()`（5天×N时段，approved/pending 分开统计，SUM CASE GROUP BY，full/willAutoReject/totalPending）；`sweepFullSlots()`（approved>=capacity 的时段把 status=0 批量驳回，幂等，auto_rejected=1 天然不占周次数——周次数是实时 COUNT 且排除 auto_rejected，零退账逻辑；capacity=0 不 sweep；读路径 fail-open）。
- **submitController.approveOne**：事务内在 quota.claim 后加时段容量校验 `COUNT(status=1 AND wbt=slot AND id!=self)`——⚠️ **必须在 where 排除自身**：此时本条已被①置为 status=1，不排除会把自己数进去永远「已满」（verify 抓出来的真 bug）。满抛 SlotFullError→40906（Codes.SLOT_FULL 新增）。approve 成功后 setImmediate(sweepFullSlots)。
- **路由**：GET /admin/submit/schedule（scheduleMatrix），注册在 /submit/:id 之前。list 接口加 slot 参数（精确筛时段）。
- **决策（用户拍板）**：时段满→自动拒绝（同名额机制）；周日提醒→要。
- **管理端 SubmitList.vue**：摘要卡加「下周排期 N 首待审 · 查看排期 ›」；slot-remind 横幅（totalPending>0 时：「请在周日 18:00 前审完」常显，实时算不搞 cron）；「下周排期」矩阵弹窗（sched-grid 5 列×N 行，格子=已排x/容量+待审徽标(amber)/已排满徽标(墨黑)，点格子→query.slot+status=0 筛待审，工具栏出可点 chip 显示当前时段）；处理台内容卡头部显示「该时段已排 x/容量 · 还可排 N/已排满」。
- **验证**：scripts/verify-slot-schedule.js 新增，23 项全绿（sweep 幂等/容量0不sweep/autoRejected不占周次数/矩阵数字/40906拒绝/文稿不受限/路由顺序）；verify-song-submit.js 回归 exit 0。部署：build+docker compose build admin-web radio-backend+up -d，容器内 grep sweepFullSlots=4 确认。
- 未做（说明过）：want_broadcast_time 加索引（校园量级几百条不需要，动 schema 收益低）。

## 时段筛选 chip 文字错位修复（用户截图：span.chip 塌了）

- 根因：slot 筛选 chip 用了 `<span class="chip">`，而 `.chip` 样式（height 30 / pill）是给状态 chip 的 `<button>`（inline-block）写的——inline 的 span 上 height/垂直居中全失效，文字塌成一截。
- 修法：span → `<button type="button" class="chip on chip-slot">`，补 `.chip-slot { white-space:nowrap }`（时段 label 长）+ em 间距 6px。
- 教训：**同一套胶囊样式换标签时必须核对 display**——button=inline-block，span=inline，height 对 inline 无效；改完走三件套（build → docker build → up --force-recreate），容器内 grep chip-slot 确认产物。

## 学生登录页间距修正（用户指出插画顶住导航栏、与方案不符）

原因：落地时 .body 顶部 padding 只有 8rpx、brand 上边距 6rpx，插画直接贴住顶栏。
修法（login.wxss）：.body 加 justify-content:center（内容在剩余空间垂直居中，同管理端登录的呼吸感），
顶部 padding 提到 24rpx，.brand 上边距 26rpx / 下边距 52rpx（插画与玻璃卡之间也拉开）。
去掉 .body 的 overflow:hidden（.page 是 min-height:100vh，小屏内容超高时页面自然滚动，不会裁切）。

## 学生登录页收音机二次重做（用户对照方案指出：机身太宽/标语位置不对/与顶栏间隔不对）

根因：我此前用 flex 拼装 + 自定尺寸（机身 300rpx），和方案比例对不上；方案 SVG viewBox 是 330×240，
**1 单位 = 1rpx 直接映射**才是正解。重写（login.wxml/wxss）：
- .radio-art 336×244rpx 单一相对容器，所有零件按 SVG 坐标绝对定位：机身 rect(75,130,180,76,r10)、
  喇叭外环 r26/内环 r15/圆心 r4.5（圆心 122,168）、旋钮 r9（cx200/232）、
  三道信号弧 = 圆心同 (165,130)、r42/64/86 的圆环只描 border-top（盒子 left=165-r, top=130-r）。
- 标语直接跟在插画下（gap 16rpx = 方案的 8px）；.body 恢复方案的顶对齐（padding 12rpx、
  brand margin 8/28rpx = 方案 4px/14px），去掉上次的垂直居中。
- 经验：**照 SVG 做 CSS 复刻时直接按 viewBox 单位 1:1 映射再整体缩放，不要目测重新构图**。

## 学生登录页按钮文字错位修复（用户 devtools 截图指出）

button.submit 文字挤出盒子：微信 button 自带默认 padding + line-height，我只设了 height/line-height 没清 padding，
行盒被默认 padding 顶下去 → 「登 录」跑到按钮外。修法：padding:0 + box-sizing:border-box + display:flex
居中 + line-height:1（ghost-btn 同步处理，去掉 line-height:84rpx）。
教训：**小程序里自定义 button 样式必须显式清 padding 并用 flex 居中**，line-height 撑高法在自带 padding 的
原生组件上必翻车。

## 模块开关名乱码修复（account_login_required 显示乱码）

- 根因：迁移 SQL 被控制台管道按 GBK 重编码后落库 → desc 双重编码坏档（源码 KNOWN_SWITCHES 是好的，
  但 listAll 以库里已有行的 desc 为准）。
- 修复：库里 desc 用 UNHEX('强制学号登录'的 UTF-8 字节) 重写（先在 backend 容器用 sequelize 写，hex 校验通过）；
  源码注册表 desc 同步改为「强制学号登录」并重启后端；迁移 SQL 文件本身也改成 UNHEX 防呆写法 + 注明
  必须带 --default-character-set=utf8mb4。HTTP 复验 desc=[强制学号登录]。
- ⚠️ 大坑合集：这台 MySQL 的 docker exec 通道 —— ① 反引号会被 PS/包装层吃掉；② ANSI_QUOTES 开启，
  双引号字符串当列名（Unknown column）；③ CHAR(... USING utf8mb4) 在该通道返回 NULL。
  可靠姿势：中文走 backend 容器 node/sequelize（hex 构造字符串），或 mysql 客户端加 utf8mb4 + UNHEX。

## 时段弹层排版修复（用户：应该适配页面）

- 根因：浮动胶囊 tabBar 是框架层，永远压在页面弹层上面 → 72vh 的弹层下沿被胶囊叠住 + 底部一大截空白，视觉上「不适配」。
- 修复：弹层/闸门都是模态——打开时 getTabBar().setData({hidden:true}) 收走胶囊（组件自带 hidden 机制，胶囊滑出屏幕），
  关闭/选中/确认时放回。时段弹层底部补「已选 / 未选择 + 提示」状态行（对应 v1 预览④的底部区），body 底部留 24rpx。
- 涉及：submit.js（tabbar(hidden) 助手，openSlots/closeSlots/pickSlot/openNotice/closeNotice/ackNotice 七个口）、
  submit.wxml（slot-sheet-foot）、submit.wxss（foot 样式 + body 底距）。
  ⚠️ 以后凡是本页新增全屏弹层，开/关都要配对调 tabbar(true/false)。

## 小程序「我的」页去掉大标题、平铺重排（用户要求）

pages/mySubmit：删掉顶部 .stack（t-page「我的」+ t-sub「投稿与审核进度都在这里」），
页面直接从用户卡开始；.body 补 padding-top:16rpx 顶住导航带（app.wxss 的 .body 自带 34rpx 纵向 gap，
卡片间距不受影响）。未登录态的 nologin 卡跟着上移。其余（segment 筛选/列表/骨架/入口/退出）结构不动。

### 学生账号导入 NaN + 文件名乱码修复（用户截图反馈，17:33）

用户反馈两处：①「确认导入 NaN 个账号」按钮；② 深色条里上传的中文文件名乱码（Â²ï¿½Ö·Ã».xlsx）。

#### 根因两个，且截图本身是旧 bundle
1. **文件名乱码 = multer latin1 坑（真后端 bug）**：busboy 按 latin1 解 multipart 的 filename，
   浏览器按 UTF-8 发的中文名全部乱码。`src/middlewares/uploadExcel.js` 的 uploadExcel 回调里加：
   `Buffer.from(req.file.originalname, 'latin1').toString('utf8')`，且仅当结果不含 U+FFFD 才覆盖
   （纯 ASCII 名转换后不变，无害）。这一步必须在 controller 的 readSheet(…, originalname) 之前 —— 中间件里做正好。
2. **NaN = 用户浏览器跑的是旧 bundle**（截图 KPI 文案「已有账号/空值行」、深色条「格式正确」都不是当前代码的文案
   —— 当前是「更新信息/跳过（已激活）/异常行」和「标准格式/紧凑格式」）。但顺手把前端做了兜底：
   `importableCount = (Number(s?.new)||0) + (Number(s?.update)||0)`（computed），按钮和 toast 都用它，
   summary 任何字段缺失都显示 0，永不 NaN。
   注意后端 analyze() 的 summary 全路径都是数字（从 {new:0,update:0,active:0,invalid:0} 起步），后端无此 bug。

#### 验证（端到端，含码点级检查）
- PowerShell 写中文名 CSV `_名册测试.csv`（年级/班级/序号/姓名 + 2 行）→ curl -F 上传 preview
- 响应 code=0、filename 码点 `540d 518c 6d4b 8bd5` = 「名册测试」（isCleanUtf8Name=true）✓
- summary = {new:1, update:0, active:1, invalid:0}（何东=new、李昭阳=active 已激活跳过）✓
- ⚠️ 验证链路坑：PowerShell 捕获 curl/node 的 stdout 按系统 GBK 解码 → 中文必显示乱码（双重编码），
  **不能靠肉眼看 JSON 文件判断编码对错，要用 Node 查 codePointAt**。

#### deploy
- admin-web + radio-backend 双容器都重建（backend 改了中间件，必须 build radio-backend，只建 admin-web 不生效）
- build 8.75s / 两容器 healthy

#### 教训
- **看截图先核对文案是不是当前代码的**（KPI 标签、状态文字对不上 = 用户在旧 bundle 上操作，先让硬刷新）
- **multer 中文文件名**：任何 multer 上传点都要做 latin1→utf8 还原（avatarUpload 用随机名存储不受影响，没动）

## 「我的」页 v1 重设计预览（用户指出整页无视觉焦点；先预览未动代码）

问题：去掉标题后整页全是同灰底卡片，无视觉落点。
方案（preview/my-page-v1/my-page-v1.html，两屏）：用户卡升级为**全页唯一深色 Hero 卡**（v8 焦点手法：
深色留给「有内容可看」的地方），内含头像（白描边首字圆）+ 姓名 + 班级·学号 + 菁悠成员徽章 +
**四格统计（全部/待审核[暖黄点亮]/已通过/已驳回，数据前端汇总零接口）**；
未登录态同位置 = 深色登录引导卡。筛选胶囊/投稿列表/入口保持羊皮纸浅色，深浅对比即层级。
待用户确认后改 mySubmit 的 wxml/wxss + js 加 status 分布汇总。

## 「我的」Hero 配色对比 v2 预览（用户要求做深灰版挑一版）

preview/my-page-v2/my-page-v2.html：同一版式三档底色并排——
A 墨黑 #272729（=管理端深色卡同源，焦点最强）、B 深灰 #3d3d42（推荐折中）、C 石墨灰 #4a4a50（焦点减弱）。
文字/统计/徽章透明度各按底色微调保对比度。待用户选定后落地 mySubmit。

## 时段弹层二返（用户：整个画面挤在左上角）

- 视觉问题两个：① 每天 2 颗小胶囊缩在左侧、右边大片空白；② 弹层写死 72vh、内容只有一半高，底部一大截空底。
- 修复：slot-chip 改 flex:1 1 auto + min-height 68rpx + 居中（≥2 个时段平分行宽，内容横向铺满）；
  弹层高度自适应——openSlots 按天数算 sheetH = clamp(34, 14 + 天数×6.5, 72) vh 写进内联样式
  （scroll-view 仍是固定高度链，不违反 iOS 显式高度的教训）。slotSheetH 默认 72。
- 小心：改 openSlots 时把 const days 定义改丢过一次（ReferenceError），--check 查不出，读回确认过。

## 「我的」Hero 浅色三案 v3 预览（用户要看浅色方案）

preview/my-page-v3/my-page-v3.html：L1 白卡大投影（海拔当焦点）/ L2 淡蓝渐变卡（呼应管理端登录底色，
统计数字走强调蓝，推荐）/ L3 白卡蓝描边（选中态语义稍歪）。头像统一墨黑首字圆收深色点。
深色三案在 my-page-v2，墨黑 v1 在 my-page-v1，均保留待选。

## 「我的」页 Hero 落地：用户选定 L1 白卡大投影

用户在深色三档（my-page-v2）与浅色三案（my-page-v3）中选定 L1。落地 mySubmit：
- wxml：用户卡换 Hero（白卡大投影 + 墨黑首字头像 + 姓名 + 班级·学号副行 heroMeta + 菁悠成员徽章 +
  四格统计 全部/待审核[暖黄]/已通过/已驳回）；未登录态同款白色浮卡当焦点。
- wxss：.hero（radius 44 + 双层重投影 0 12/28 -12 与 0 44/88 -36）、.hero-stats 四格（分隔线 + tabular-nums）、
  .nologin 同海拔。
- js 增量：stats {all,pending,passed,rejected} 由 fetchStats() 单独拉一次 /user/submit/my pageSize=200 前端汇总
  （零后端改动，失败静默）；heroMeta = [className, username] 拼接；doLogout 复位 stats/heroMeta。
- 后续待用户在开发者工具里目检。

### 批量导入预览屏按 v8 重做（用户贴设计稿指出"导入预览模块做错了"，17:43）

对照 preview/admin-ui-v8/admin-ui-v8.html 第 2618-2707 行（data-title=批量导入 · 预览）整段重写。

#### v8 屏结构与文案（逐项对齐）
- **标题区**：「批量导入 · 预览」+ 副标「filename · 已解析 N 行 · 表头识别：第 X 行」+ 右侧「重新选择」
- **步骤条卡**：① 上传表格 › ② 预览与确认（当前加粗）› ③ 完成；圆号 23px pill（ink 黑=已到 / 灰=未到 / ✓=已过）；
  右侧 micro「只解析不落库，确认后才写入」
- **统计条卡**（替代旧大数字 KPI）：新建(tag 绿) / 覆盖(tag 琥珀)+「未激活，可安全覆盖」/
  受保护(tag 灰)+「已激活，默认跳过」/ 异常(tag 红)+「带原文行号，不写库」
- **表格 5 列**：行号(第 X 行) / **表格原内容**(raw 单元格 ' · ' 拼接——替代旧的结构化 8 列) /
  识别结果(username) / 结论(四色标签) / 说明(按 state 生成文案)；异常行红底 #fffafa
- **底部 cols**：左卡=强制覆盖已激活(el-switch，默认关)+严格模式(el-switch，默认开，说明带当前异常数)；
  右列=红底警示条「导出文件含初始密码明文…」+按钮组「取消 / 忽略异常行继续(link, 仅异常>0) / 确认导入 N 行」

#### 顺手修的真 bug
- **STATE_TAG 键名错误**：前端写 `create`，后端 state 是 `'new'` → 新建行标签一直显示英文「new」灰色。
  改 CONCL{new/update/active/invalid}。
- 「确认导入」支持 strict 参数（后端 roster.commit 本来就收）：主按钮 strict=true（异常时被后端拦）、
  「忽略异常行继续」strict=false。
- impForce 切换会**重传同一文件重新 preview**（lastFile ref）——force 影响 new/update/active 分布，旧数据不能复用。

#### 验证（真实 UI 链路）
- **agent-browser 有 `upload <sel> <files...>` 原生命令**（--help 可查全部命令），直接往 hidden input 塞文件
  触发 Vue onPick → 预览屏真实渲染 —— 不用再绕 fetch 注入。
- 测试 CSV 6 行（已激活/新建/缺序号/批内重复/未激活×2）→ 探针全对：标题/副标(文件名中文正常)/
  统计条 4 标签 1-2-1-2 / 表格 6 行结论 [受保护,新建,异常,异常,覆盖,覆盖] / 按钮 [取消,忽略异常行继续,确认导入 3 行]。
- vite build 9.27s；admin-web 容器 healthy。截图 preview/admin-ui-v8/shots/v8-live-05-import-preview.png。

#### 教训
- **改前先 grep 设计稿原文**（这屏文案「受保护/覆盖/不写库/整批会被拦下」全部来自 v8 原文，不要自己造词）
- agent-browser 完整命令集用 `--help` 看（upload/download/drag/find/network 都有），别再猜「没有这功能」

## 真机登录连不上排查
- 报错链：request:fail url not in domain list → 改 app.js baseURL 为 http://172.17.63.186/api → ERR_CONNECTION_REFUSED。
- 已排除：nginx 绑 0.0.0.0:80 正常、Docker 防火墙规则在 Public 且范围 Any、本机 curl http://172.17.63.186/api/health 返回 404（nginx 有应答）。
- 已做：新增防火墙入站规则 Radio Dev HTTP 80（TCP 80，全 Profile）。
- 结论：服务端 OK，剩余问题在手机侧网络（同网段/AP 隔离），需手机浏览器测 http://172.17.63.186。

## 腾讯云轻量服务器部署完成（用户动手，我导航教学）

- 服务器：腾讯云轻量 lhins-rw8dtsdi，公网 129.28.26.180，Ubuntu 24.04 + Docker CE 27.5.1，2C2G+2G swap，免费体验 1 个月
- 流程：tar 打包（排除 node_modules/.git/preview/tests/data/docs）→ scp 上传 → 服务器 ~/radio → docker compose up -d --build
- 踩坑与修复（都已验证）：
  1. 后端 Dockerfile 原是「COPY 本机 node_modules」写法 → 改为容器内 `RUN npm ci --omit=dev --registry=mirrors.cloud.tencent.com/npm/`，.dockerignore `!node_modules/` 改为 `node_modules`（本机两文件已同步改）
  2. `tar --exclude=docs` 按目录名匹配，误杀了 src/docs/swagger.js（app.js require 它）→ 单独 scp 补传；第一次 scp 实际没传上（无输出=失败），重传成功
  3. ubuntu 不在 docker 组 → sudo usermod -aG docker
- 上传素材：本机 uploads/avatars（27 文件）已放到服务器 ~/radio/data/uploads（compose 挂载点）
- miniprogram/app.js baseURL 已切到 http://129.28.26.180/api（调试期 IP，正式化后换域名）
- 服务器库是从 schema.sql 全新初始化（只有 seed + teacher/admin123456 初始管理员）；本机数据（名册/点歌设置/注意事项）是否迁移待用户确认
- 后续待办：域名+ICP 备案（免费体验机不足 3 个月不能备案，要买年付机）→ HTTPS 证书 → 微信合法域名 → 体验版
## 风采展示排查（用户以为后端没做好）

- 实测结论：后端是好的 —— /user/showcase 200，社干 2 人（张三…）、部员 3 人分主持部/播音部/编辑部，
  结构与小程序期望一致（cadre + staff{dept:[]}），isShow 过滤、/user/carde/:id、/user/staff/:id 都在。
- 真正的问题：**部分成员没传照片（avatar 空，直接插库的测试数据）**，而 members/memberDetail 的
  <image src=\"\"> 没有兜底 → 渲染成空白块，看起来像「没做」。
- 修复：members.js/memberDetail.js 给每人算 initial（姓名首字）；两页 wxml 空头像分支渲染
  首字兜底块（members：墨底白字大方块；memberDetail：墨底白字大圆）；样式补 .mphoto-fallback /
  .hero-photo-fallback。纯小程序端，重新编译生效。
- 排查习惯：先打接口再下结论（curl/Invoke-WebRequest + 查表行数），「以为后端没做」多数是前端兜底缺失。

## Git 发布流程建立（2026-09-19 晚）

- 本机：.gitignore 重写（修复行内注释不生效 bug；deploy/certs 私钥从跟踪移除；_*/tar 包排除）；提交 f983210 推送到 GitHub 私有仓库 Nailoonger/radio-backend（master 分支，Credential Manager 浏览器授权）
- 服务器：ed25519 部署密钥（Deploy key 只读）→ 重整目录：旧 ~/radio 改名 radio-old（回滚保险），git clone 到 ~/radio，data/（MySQL 数据+uploads）与 .env 从旧目录搬回，docker compose up -d 重建
- admin-web Dockerfile 改多阶段构建（node:20-alpine npm ci + vite build → nginx 拷 dist），新增 admin-web/.dockerignore；服务器 2G 内存 vite build 实测可跑
- 验证：/api/health 返回后端自定义 40401（nginx→backend 链路通；/health 在根路径不在 /api 前缀下）
- 发布流程定型：本机 add/commit/push → 服务器 cd ~/radio && git pull && docker compose up -d --build
- 本机 MySQL 数据已迁服务器库（254 学生/3 管理员）；本机 Docker 已 stop（数据保留）
## ⚠️ 收尾时遗留的未解决问题（下个会话优先处理）

- **风采照片「可选」改动后端未生效**：cadre/staffController 已去掉必填校验（源码正确，node --check 过），
  但 docker compose build radio-backend + force-recreate 后，接口仍返回旧文案 40001「请上传或填写头像 URL」。
  疑似镜像没吃到新代码（build 缓存 / COPY 层未失效）。下一步：docker exec radio-backend grep '请上传或填写'
  /app/src/controllers/admin/cadreController.js 确认容器内文件；若仍旧 → docker compose build --no-cache radio-backend。
- **影响面**：管理端 Showcase 表单已放开必填（新 bundle 已上线），但保存无照片成员会被后端 40001 拒 →
  管理端实际仍必须填照片 URL；小程序端首字兜底不依赖后端，已生效。
- **连带发现**：docker compose restart 不更新镜像代码（后端源码改动必须 build + force-recreate）；
  radio-nginx 曾在一次 admin-web 重建后自动退出（Exited 0），80 端口全拒——排障先查它；start 后恢复。
- 其余今日事项均已完成并验证（详见上文各节）。
